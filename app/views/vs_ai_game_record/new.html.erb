<% provide(:title, 'VsAiGameRecord New') %>
<% provide(:class_text, 'vs_ai_game_record--new') %>
<% provide(:button_text, '作成') %>
<div class="jumbotron bg-image">
  <div class="bg-mask">	
    <h1>プレイ情報登録</h1>
    <div class="row">
      <div class="col-md-6 col-md-offset-3">
        <% if @your_move == "first" %>            	  
          <%= render 'vs_ai_first_form' %>
        <% elsif @your_move == "second" %>
          <%= render 'vs_ai_second_form' %>
        <% end %>
      </div>
    </div>
  <div>
</div>
