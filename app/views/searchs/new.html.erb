<% provide(:title, 'Search New') %>
<% provide(:class_text, 'search--new') %>
<% provide(:button_text, '作成') %>
<div class="jumbotron bg-image">
  <div class="bg-mask">
    <h1>プレイ情報登録</h1>
    <div class="row">
      <div class="col-md-6 col-md-offset-3">
        <%= render 'form' %>
      </div>
    <div>
  </div>
</div>
